/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  printWidth: 80,
  tabWidth: 2,
  semi: false,
  trailingComma: 'all',
  singleQuote: true,
  arrowParens: 'always',
}

export default config
